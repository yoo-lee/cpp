#ifndef PMERGE_ME_HPP
#define PMERGE_ME_HPP

#include <vector>
#include <list>
#include <algorithm>

class PmergeMe {
public:
  std::vector<int> sort_vector(const std::vector<int> &data);
  std::list<int> sort_list(const std::vector<int> &data);  
};

#endif // PMERGE_ME_HPP
