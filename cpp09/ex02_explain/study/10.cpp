    std::vector<int> vec;


for (int i = 1; i < c; i++;)
	try
	{
		for (int j = 0; v[i][j]; j++;)
			if  (v[i][j] > '9' || v[i][j] < '0')
				std::cout << "bad input" << std::endl;
				return 1;
		}
	}