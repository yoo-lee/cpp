

list f(list )
void mergeList(std::list<int>& A, int p, int q, int r) 
{
	for (int i = 0; i < n1; i++;)
		LA.push_back(*std::next(A.begin(),p+i ))
}

int main ()
{
	int vector<int> a ={1, 2, 3};
	int list<int> a ={1, 2, 3};
	std::next(1,3);

	
}