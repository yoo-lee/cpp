B();

B(const std::string &name, int grade)
{
	checkGrade();
	if (t_bool)
	std::cout << "standard constructor for B called"
	_name = "";
	_grade = lowestGrade;
}

B(const B &o)
{
	checkGrade();
	if (t_bool)
	cout << " Copy constructor";
	*this -> obj;
}

~Bureaucrat