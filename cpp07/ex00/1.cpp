
void swap(int a, int b)
{
	int tmp;
	{
		tmp = a;
		a = b;
		b = tmp;
	}	
}

// • min: Compares the two values passed in its arguments and returns
//  the smallest one.
//  If the two of them are equal, then it returns the second one.
int min(int a, int b)
{
	if (a > b)
	return b;
	if (a = b)
		retnr 

}

int min ()
{

}  
// Implement the following function templates:
// • swap: Swaps the values of two given arguments. 
// Does not return anything.

// • max: Compares the two values passed in its arguments and returns the greatest one.
// If the two of them are equal, then it returns the second one.
// These functions can be called with any type of argument. The only requirement is
// that the two arguments must have the same type and must support all the comparison
// operators.