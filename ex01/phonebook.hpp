class Phonebook
{
    std::string  name;   // 名前
    int          grade;  // 学年
    int          score;  // 得点
    int          adana;  // 得点

public:
    void add(std::string name, int grade, int score);
    void change(std::string name, int grade, int score);
    void exit(std::string name, int grade, int score);
};

class Contact
{
	
}
