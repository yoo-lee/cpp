#include <iostream>
using namespace std;

int main (int ac, char **av)
{
	int i = 0;
	char s;

	if (ac == 1)
	cout << "* LOUD AND UNBEARABLE FEEDBACK NOISE *" << endl;

	if (ac == 2)
		{
		int m[i];
		int n;
		printf("1");
		int i, j;
		i = 0;
		j = 0;

		while(m[i])
		{
			m[i] = *av[i];
			int n = (char *)n;
			// cout << n << endl;
			s = (char)toupper(m[i]);
			n[j] = s;
			cout << n[j];
			i++;
			j++;
		}
}