#include<iostream>
#include<string>

using namespace std;
bool f(string T)
{
('A' =< T && 'Z' >= T)
}

int main ()
{
    stiring S,T;
    
    cin << S;

    if(S.find())
    {return(YES)}
    else
    return(NO)
}