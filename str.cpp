#include <stdio.h>
#include <iostream>
#include <string>
// #include <locate>

char ft_toupper(char c)
{
    return(c - ('Z' - 'z'));
}

int main (int ac, char **av)
{
    int i,j;
    i = 0;
    j = 0;
    if (ac == 1)
    std::cout <<
    if (ac < i)
    {
    std::string str(av[i]);
    i++;
    std::cout << str;
    }
    // n = ft_toupper(m);
}
